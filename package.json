{
  "name": "medieval-mercenary-management-rpg",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc",
    "test": "vitest run",
    "test:smoke": "node scripts/smoke-test.mjs",
    "generate-mask": "node scripts/generate-map-mask.mjs",
    "check:bundle": "node scripts/check-bundle.mjs",
    "check": "npm run typecheck && npm run test && npm run build && npm run test:smoke && npm run check:bundle"
  },
  "devDependencies": {
    "jimp": "^1.6.1",
    "playwright": "^1.61.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  }
}
